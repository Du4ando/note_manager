def main(): # Создаем функцию.
    заголовки = [] # Создаем пустой список, чтобы хранить заголовки, которые введет пользователь
    while True: # Начинаем бесконечный цикл, который будет продолжаться, пока мы его не остановим
        заголовок = input("Введите заголовок (или оставьте пустым для завершения): ")
        if заголовок == "": # Если пользователь ввел пустую строку, выходим из цикла
            break
        заголовки.append(заголовок) # Добавляем заголовок в список заголовков
    print("\nЗаголовки заметки:")  # После выхода из цикла проверяем, есть ли добавленные заголовки
    for заголовок in заголовки:
        print(f"- {заголовок}")
main() #Запуск функции
